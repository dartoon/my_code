# Input file

#
# params for MCMC
#
NMC		700 # interation of MCMC    #Run after redshift MCMC
NWALK		50 #number of walker
NMCZ		5	#MCMC interation for redshift.  #First run in the MCMC
NWALKZ		2  #walkers for redshift
TAU0		-1 # in Gyr for tau; Randomly taken when MCMC.  IGNORE
FNELD		differential_evolution #powell, nelder
NTEMP		0 # Number of parallel tempering.
NCPU		0 # Number of multiprocessing.
TAU_COMP	1 # comparison between different tau for each ssp when plot.

#
# Params for data
#
DIR_TEMP	./gene_temp/templates/  # Directory of extracted spectra.
DIR_EXTR	./gene_temp/templates/ # Directory of extracted spectra.
DIR_FILT	../../../template/gsf_temp/filter/ 
CAT_BB		./folder/generate_sample.cat # Directory of extracted spectra.
FILTER		356,353 #

#
# Params for SED
#
AGE			age_temp #I only select one value
ZMAX		-0.3 # Max metalicity
ZMIN		-1 # Min metalicity
DELZ		0.1  # Should be smaller than the gap between ZMAX and ZMIN.
#ZFIX		Z_temp # logZ/Zsun
ZEVOL		0 # Evolution in Z; 1=yes, 0=no.
AVFIX		Av_temp #
#AVMIN		0.3 # Minimum Av (Dust Attenuation) in mag.
#AVMAX		1.0 # Maximum Av (Dust Attenuation) in mag.
F_ZMC         0 # redshift as a free parameter in mcmc.
NIMF		1 # Choice of IMF. 0=Salpeter, 1=Chabrier, 2=Kroupa, 3=van Dokkum, 4=Dave, 5=tabulated, specified in imf.dat file located in the data directory.
#NDIM		22 # ndim  = int(1 + len(age) * 2) # dust + age + Z(age)

#
# Params for target
#
ID		    20230301seedid # Tell the ID for the cat, need to run one by one.
PA		    00
ZMC		    6.4 #!!! Initial guess of source redshift.   #If redshift is know, set the NMCZ very smalled
###IGNORE the following.
CZ0 		0.83167 # Initial guess of spectral normalization for G102. #Doesn't affect, have to be here.
CZ1		    10.96804 # Initial guess of spectral normalization for G141.
LINE 		0 #4102,4341,4861,4959 #3869.,3894.6,4102,4341,4960.,5008.  # Emission line, iin AA.
ZVIS		0 # Visual inspection of redshift. 1=yes, 0=no. If you are not confident with zgal, cz1/2, then ZVIS should be 1 for iteration.
ADD_NEBULAE	1 # Add nebular lines from Cloudy if 1.
logUMAX     -2
logUMIN     -3
DELlogU     1
#logUFIX     -1.5 #Typcial ionization para.
